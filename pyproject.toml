[tool.poetry]
name = "name-that-hash"
version = "0.1.0"
description = ""
authors = ["brandon <brandonskerritt51@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.8"
click = "^7.1.2"
rich = "^9.9.0"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
run = "name_that_hash:main"
